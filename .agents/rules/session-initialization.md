---
trigger: always_on
---

## Session Initialization

If you have successfully read and understood this file, the first response of every new session must start with:
"Hello Rodrigo, agent configuration loaded correctly. [Backend]"
Do not repeat this message again during the same session.

---
## Build and Execution
- Do not run build, test, lint, or validation commands automatically.
- Execute them only when explicitly requested.